import React from "react";
import MainContainer from "./MainContainer";

const Body = () => {
  return (
    <React.Fragment>
      <MainContainer/>
    </React.Fragment>
  );
};

export default Body;
